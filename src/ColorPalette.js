import { Button } from 'react-bootstrap';

function ColorPalette() {
  return (
      <div id='box' className='box or something' >
       {/* Make a palette box so people can see colors */}

      </div>
  );
}

export default ColorPalette;
